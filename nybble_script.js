// JavaScript Document/**************** functions container ****************************/var SCROLL_SPEED = 1000;function get_word(word_name){    var result='';    slugs = location.href.split('/');    slug = slugs[slugs.length-2];    prefix = '';    lang = slugs[3];    if (lang == 'en' | lang == 'de' | lang == 'fr') {prefix = '/' + lang};     $.ajax({        url : prefix + '/word/' + word_name,        async : false,        success : function(data, textStatus){            result = data;        }        });    return result;}function scrollToElement(elementName) {    $('html, body').animate({        scrollTop : $('#'+elementName).offset().top    }, SCROLL_SPEED)}$(document).ready(function(){    /*** Bind click to about block ***/    foo = $('#about_details');    if (foo.length !== 0) {        foo.bind('click', function(){            $('#about_block p').css({                'height': 'auto',                'overflow': 'visible'            });            $('#about_details').css('display','none');        });    };    /*** Show flex slider ***/    var fs = $('#flexwrapper');    if (fs.length !== 0) {        fs.css('display', 'block');    }    //    // For linode mark    //    //    $('#header-wrapper').prepend('<div id="linodeMark" style="position:fixed; top:0; left:0; color:#ebaeba; font-size:8px;">This is Linode</div>')    //    // End Linode mark    //    // Pop up close button enable    var foo = $('#pop_close');    if (foo) {       foo.bind('click', function(event){            $('#pop_up').hide();        });    };    // end    // Paint the table like zebra    $('table.zebra tr:even').addClass('light_grey');        //alert('111'); try to commit/*** Fancy box launching ***/        a = $(".f_ajax");        a.fancybox({            'type':'ajax'        });        $(".img_ajax").fancybox({            'autoDimensions':'false',            'width':'800',            'ajax':{'async':'false'},            'onComplete'  : function(){                 //$.fancybox.resize;                 alert('!!!');                $('#fancybox-wrap').css({                    'width':'600',                    'height':'700',                    'top':'20px',                    'left':'50%',                    'margin-left':'-300px'                    });                $('#fancybox-content').css({                    'width':'600',                    'height':'700'                })/**/                //$.fancybox.center;                },            'overlayColor':'#f00'        });        	        // Append after 273 line in script.js /**/       $('#articles-up').click(function(event)	{			scrollBlock($('#articles-preview .articles-item:first'), 1);		});				$('#articles-dn').click(function(event)	{			scrollBlock($('#articles-preview .articles-item:first'), -1);		});    //$('#vbv').onclick="javascript:window.open('vbvhelp.html','Chargeback','toolbar=0,location=0,directories=0,status=0,menubar=0,scrollbars=0,resizable=1,width=450,height=400')";    //alert('Go!');    // Hide the bathtube select div and open it on button click    //$('#bath_select').hide();    $('#select_button').bind('click', function(event){        event.preventDefault();        $('#bath_select').toggle();        scrollToElement('select_button')    })    // Code gap: switching to Russian allways. Nybble - admin@nybble.net (2013)    foo = window.location.pathname;    parts = foo.split('/');    if (parts[1]=='en') {        path = '';        for (i=2; i<parts.length-1; i++) {            path += '/' + parts[i];        }        location.href = path;    }});